# Messaging Platform Runbook

Internal runbook for candidate email delivery. Escalate broad invitation failures, provider suppression spikes, template token failures, and sender-domain authentication issues. Attach tenant ID only internally.
