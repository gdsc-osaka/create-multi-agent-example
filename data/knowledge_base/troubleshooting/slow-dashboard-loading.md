# Slow Dashboard Loading

Dashboard latency is often caused by very large saved filters, heavy date ranges, or regional query load.

Collect workspace ID, dashboard URL, approximate load time, affected users, and browser console errors.
