# CSV Candidate Import FAQ

Bulk imports use saved field mappings, required candidate identifiers, and dedupe rules. Always compare preview counts, skipped rows, and sanitized sample records before rerunning an import.
